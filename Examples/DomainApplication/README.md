# DomainApplication

This example still hand-writes the repository and CRUD logic, but now the commands, queries, handlers, results, and repository contract come from Thessera packages consumed through the local NuGet feed.

Refresh the feed first, then run `dotnet run --project Examples\DomainApplication`. Run its companion test with `dotnet test Examples\DomainApplication.Tests`.
